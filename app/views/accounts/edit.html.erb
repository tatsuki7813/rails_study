<% @page_title = "アカウント情報の編集" %>
<h1><%= @page_title %></h1>

<div class="toolbar"><%= link_to "マイアカウントに戻る", :account %></div>

<%= form_for @member, url: :account, as: "account" do |f| %>
  <%= render "shared/member_form", form: f %>
  <div><%= f.submit %></div>
<% end %>
